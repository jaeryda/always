<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.always.mapper.PostMapper">

    <resultMap id="PostResultMap" type="com.always.entity.Post">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="imagePath" column="image_path"/>
        <association property="author" javaType="com.always.entity.User">
            <id property="id" column="author_id"/>
            <result property="username" column="author_username"/>
            <result property="email" column="author_email"/>
        </association>
    </resultMap>

    <!-- 전체 조회 -->
    <select id="findAll" resultMap="PostResultMap">
        SELECT 
            p.id,
            p.title,
            p.content,
            p.created_at,
            p.updated_at,
            p.image_path,
            p.author_id,
            u.username AS author_username,
            u.email AS author_email
        FROM posts p
        LEFT JOIN users u ON p.author_id = u.id
        ORDER BY p.created_at DESC
    </select>

    <!-- ID로 조회 -->
    <select id="findById" parameterType="Long" resultMap="PostResultMap">
        SELECT 
            p.id,
            p.title,
            p.content,
            p.created_at,
            p.updated_at,
            p.image_path,
            p.author_id,
            u.username AS author_username,
            u.email AS author_email
        FROM posts p
        LEFT JOIN users u ON p.author_id = u.id
        WHERE p.id = #{id}
    </select>

    <!-- 제목으로 검색 -->
    <select id="findByTitleContaining" parameterType="String" resultMap="PostResultMap">
        SELECT 
            p.id,
            p.title,
            p.content,
            p.created_at,
            p.updated_at,
            p.image_path,
            p.author_id,
            u.username AS author_username,
            u.email AS author_email
        FROM posts p
        LEFT JOIN users u ON p.author_id = u.id
        WHERE p.title LIKE CONCAT('%', #{title}, '%')
        ORDER BY p.created_at DESC
    </select>

    <!-- 제목 또는 내용으로 검색 (페이지네이션) -->
    <select id="findByTitleContainingOrContentContaining" resultMap="PostResultMap">
        SELECT 
            p.id,
            p.title,
            p.content,
            p.created_at,
            p.updated_at,
            p.image_path,
            p.author_id,
            u.username AS author_username,
            u.email AS author_email
        FROM posts p
        LEFT JOIN users u ON p.author_id = u.id
        WHERE p.title LIKE CONCAT('%', #{keyword}, '%')
           OR p.content LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY p.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 제목 또는 내용으로 검색 개수 -->
    <select id="countByTitleContainingOrContentContaining" parameterType="String" resultType="int">
        SELECT COUNT(*)
        FROM posts
        WHERE title LIKE CONCAT('%', #{keyword}, '%')
           OR content LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <!-- 전체 개수 -->
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM posts
    </select>

    <!-- 페이지네이션 -->
    <select id="findAllWithPagination" resultMap="PostResultMap">
        SELECT 
            p.id,
            p.title,
            p.content,
            p.created_at,
            p.updated_at,
            p.image_path,
            p.author_id,
            u.username AS author_username,
            u.email AS author_email
        FROM posts p
        LEFT JOIN users u ON p.author_id = u.id
        ORDER BY p.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 작성자 ID로 조회 (페이지네이션) -->
    <select id="findByAuthorId" resultMap="PostResultMap">
        SELECT 
            p.id,
            p.title,
            p.content,
            p.created_at,
            p.updated_at,
            p.image_path,
            p.author_id,
            u.username AS author_username,
            u.email AS author_email
        FROM posts p
        LEFT JOIN users u ON p.author_id = u.id
        WHERE p.author_id = #{authorId}
        ORDER BY p.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 작성자 ID로 조회 개수 -->
    <select id="countByAuthorId" parameterType="Long" resultType="int">
        SELECT COUNT(*) FROM posts WHERE author_id = #{authorId}
    </select>

    <!-- 작성자 ID로 조회 (리스트) -->
    <select id="findAllByAuthorId" parameterType="Long" resultMap="PostResultMap">
        SELECT 
            p.id,
            p.title,
            p.content,
            p.created_at,
            p.updated_at,
            p.image_path,
            p.author_id,
            u.username AS author_username,
            u.email AS author_email
        FROM posts p
        LEFT JOIN users u ON p.author_id = u.id
        WHERE p.author_id = #{authorId}
        ORDER BY p.created_at DESC
    </select>

    <!-- 저장 -->
    <insert id="insert" parameterType="com.always.entity.Post" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO posts (title, content, created_at, updated_at, image_path, author_id)
        VALUES (#{title}, #{content}, #{createdAt}, #{updatedAt}, #{imagePath}, 
                #{author.id, jdbcType=BIGINT})
    </insert>

    <!-- 업데이트 -->
    <update id="update" parameterType="com.always.entity.Post">
        UPDATE posts
        SET title = #{title},
            content = #{content},
            updated_at = #{updatedAt},
            image_path = #{imagePath},
            author_id = #{author.id, jdbcType=BIGINT}
        WHERE id = #{id}
    </update>

    <!-- 삭제 -->
    <delete id="deleteById" parameterType="Long">
        DELETE FROM posts WHERE id = #{id}
    </delete>

    <!-- 존재 여부 확인 -->
    <select id="existsById" parameterType="Long" resultType="boolean">
        SELECT COUNT(*) > 0 FROM posts WHERE id = #{id}
    </select>

</mapper>

