<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.always.mapper.MonthlyBudgetMapper">

    <resultMap id="MonthlyBudgetMap" type="com.always.entity.MonthlyBudget">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="categoryId" column="category_id"/>
        <result property="year" column="year"/>
        <result property="month" column="month"/>
        <result property="amount" column="amount"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUserIdAndYearMonth" resultMap="MonthlyBudgetMap">
        SELECT id, user_id, category_id, year, month, amount, created_at, updated_at
        FROM monthly_budgets
        WHERE user_id = #{userId} AND year = #{year} AND month = #{month}
        ORDER BY id DESC
    </select>

    <select id="findByUniqueKey" resultMap="MonthlyBudgetMap">
        SELECT id, user_id, category_id, year, month, amount, created_at, updated_at
        FROM monthly_budgets
        WHERE user_id = #{userId} AND category_id = #{categoryId} AND year = #{year} AND month = #{month}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.always.entity.MonthlyBudget" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO monthly_budgets (user_id, category_id, year, month, amount, created_at, updated_at)
        VALUES (#{userId}, #{categoryId}, #{year}, #{month}, #{amount}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.always.entity.MonthlyBudget">
        UPDATE monthly_budgets
        SET amount = #{amount},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteByUniqueKey">
        DELETE FROM monthly_budgets
        WHERE user_id = #{userId} AND category_id = #{categoryId} AND year = #{year} AND month = #{month}
    </delete>

</mapper>

